{
  "extends": "next/core-web-vitals",
  "rules": {
    "react-hooks/rules-of-hooks": "off", // Disable hooks rule
    "react-hooks/exhaustive-deps": "warn" // Change this to "off" if you want to disable it
  }
}
